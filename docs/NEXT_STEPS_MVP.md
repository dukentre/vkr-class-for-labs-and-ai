# Состояние и следующие шаги MVP

## Уже сделано

1. Добавлены `templates/course`, `templates/practice`, `templates/lab`.
2. Добавлен Node.js backend с endpointами из `API_MVP.md`.
3. Добавлен UI: список проектов, документы, кнопка сборки, PDF-превью, лог.
4. Добавлены `Dockerfile` и `docker-compose.yml` с TeX Live.
5. Добавлен первый шаблон лабораторной работы.

## Дальше

1. Поднять Docker Desktop и проверить реальную PDF-сборку внутри контейнера.
2. Доточить тексты шаблонов под реальные требования преподавателя.
3. Добавить кнопку открытия физической папки проекта, если приложение запускается локально.
4. Добавить endpoint удаления тестовых проектов/документов.
5. Добавить watcher или ручную кнопку refresh для PDF после правок ИИ-агента.

Первый полезный результат: ИИ-агент меняет `.tex`, вызывает `POST /api/build`, а в браузере сразу появляется обновленный PDF.
