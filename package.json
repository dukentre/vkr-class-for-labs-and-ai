{
  "name": "swsu-latex-lab",
  "version": "0.1.0",
  "private": true,
  "description": "MVP web interface and build API for SWSU LaTeX projects.",
  "scripts": {
    "start": "node app/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
